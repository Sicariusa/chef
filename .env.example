# Multi-Tenant Chef Environment Variables

## Required: Convex Configuration
# Your Convex deployment URL (from `npx convex dev`)
VITE_CONVEX_URL=https://your-deployment.convex.cloud

## Required: Convex OAuth (for project creation)
# Create an OAuth app at https://dashboard.convex.dev/team/settings/applications/oauth-apps
CONVEX_OAUTH_CLIENT_ID=your-convex-oauth-client-id
CONVEX_OAUTH_CLIENT_SECRET=your-convex-oauth-client-secret
PROVISION_HOST=https://api.convex.dev

## Required: Authentication Provider (choose one or both)
# Google OAuth (recommended)
AUTH_GOOGLE_ID=your-google-client-id
AUTH_GOOGLE_SECRET=your-google-client-secret

# GitHub OAuth (optional)
AUTH_GITHUB_ID=your-github-client-id
AUTH_GITHUB_SECRET=your-github-client-secret

## Optional: AI Model API Keys
# Anthropic (Claude models)
ANTHROPIC_API_KEY=your-anthropic-api-key

# Google AI
GOOGLE_API_KEY=your-google-ai-api-key

# OpenAI (GPT models)
OPENAI_API_KEY=your-openai-api-key

# xAI (Grok models)
XAI_API_KEY=your-xai-api-key

# OpenRouter (access to multiple models)
OPENROUTER_API_KEY=your-openrouter-api-key

## Optional: Monitoring & Analytics
# Sentry (error tracking)
VITE_SENTRY_DSN=your-sentry-dsn

# PostHog (analytics)
VITE_POSTHOG_KEY=your-posthog-key
VITE_POSTHOG_HOST=your-posthog-host

# LaunchDarkly (feature flags)
VITE_LD_CLIENT_SIDE_ID=your-launchdarkly-client-id

## Optional: Admin
# Admin token for debugging endpoints
CHEF_ADMIN_TOKEN=your-admin-token
